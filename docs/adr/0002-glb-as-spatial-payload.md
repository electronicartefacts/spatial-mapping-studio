# ADR 0002: GLB is the spatial payload

**Decision:** V0 uses glTF 2.0 GLB rather than a proprietary geometry format. **Consequences:** the artefact adds semantics without replacing industry tooling.
