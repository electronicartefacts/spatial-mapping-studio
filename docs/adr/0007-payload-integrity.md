# ADR 0007: Payload integrity

Triangle indices only describe a precise GLB. V0.1 may store SHA-256 of exact bytes so a Viewer can reject modified payloads. Existing manifests without a hash remain readable.
