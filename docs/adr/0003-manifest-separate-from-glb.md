# ADR 0003: Keep manifest separate from GLB

**Decision:** semantic data lives in `artifact.json` beside `model.glb`. **Consequences:** V0 is transparent and portable; a standard ZIP package may be specified later.
