# Spatial Artefact V0.1 conformance corpus

All manifests use the shared deterministic `model.glb`. `valid-basic` and `valid-no-integrity` must load; the remaining directories are negative cases with a precise expected failure.
