# Runtime GLB fixtures

`multi-primitive.glb` contains one mesh with two primitives and distinct materials. `shared-material.glb` contains the same topology with one material shared by both primitives.

They are deterministic, tiny GLB 2.0 fixtures generated from `scripts/generate-advanced-fixtures.mjs` with:

```bash
node scripts/generate-advanced-fixtures.mjs
```
