# Contributing

Spatial Mapping Studio is local-first by default. Keep GLB and manifest handling in-browser, avoid mandatory services, and preserve framework-free public APIs.

Run `pnpm validate` before opening a pull request. Do not add large source datasets to the repository; use small reproducible fixtures instead.
